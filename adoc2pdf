#!/bin/bash
FILE="${1}"
DOC="$(echo ${FILE%.*})"
CSS="$HOME/.cfg/css/custom.css"
FTR="$HOME/.cfg/nofooter.conf"

asciidoc -b html5 -a data-uri -a stylesheet=${CFG} -f ${FTR} "${DOC}.adoc"
wkhtmltopdf -s letter --footer-center "Page [page] of [toPage]" "${DOC}.html" "${DOC}.pdf"
rm "${DOC}.html"


