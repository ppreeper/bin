#!/bin/bash
FILE="${1}"
DOC="$(echo ${FILE%.*})"
CSS="$HOME/.cfg/css/asciidoc.css"
FTR="$HOME/.cfg/nofooter.conf"

asciidoctor -b html5 -a stylesheet=${CFG} "${DOC}.adoc"
wkhtmltopdf -s letter --footer-center "Page [page] of [toPage]" "${DOC}.html" "${DOC}.pdf"
rm "${DOC}.html"


